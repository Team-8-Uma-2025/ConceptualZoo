{
    "version": 2,
    "outputDirectory": "frontend/build",
    "buildCommand": "cd frontend && CI=false npm run build",
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/index.html" }
    ]
  }